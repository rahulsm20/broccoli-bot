package middleware

import (
	"github.com/gin-gonic/gin"
)

func CheckSpotifyAuth(c *gin.Context) {
	// client := &http.Client{}
	// req, err := http.NewRequest("GET", "https://example.com", nil)
	// if err != nil {
	// 	fmt.Println("Error creating request:", err)
	// 	return
	// }
	// req.Header.Set("Authorization", fmt.Sprintf("oauth:%v",accessToken))
}
